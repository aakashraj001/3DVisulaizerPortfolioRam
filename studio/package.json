{
  "name": "bunq-labs-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BUNQ Labs — a bold, pop Digital Design & Visualisation Studio portfolio (variant two).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
