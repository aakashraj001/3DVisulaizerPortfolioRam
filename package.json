{
  "name": "cinematic-visualizer-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A dark, cinematic, CMS-driven portfolio for a 3D / architectural visualizer — GSAP ScrollSmoother + Three.js depth hero.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 5050"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
